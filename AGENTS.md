# AGENTS.md

## Cursor Cloud specific instructions

This repository ("AMETSEONEW") is currently an empty/bare repository with only a `README.md`. There is no application code, no build system, no dependencies, and no services to run.

- **No services to start.** There are no backend, frontend, or database services.
- **No tests to run.** There is no test framework or test files.
- **No lint/build commands.** There is no linter or build configuration.
- **No dependency installation required.** There are no `package.json`, `requirements.txt`, or similar files.

Once application code is added, update this section with relevant startup, test, lint, and build instructions.
